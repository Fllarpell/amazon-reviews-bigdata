# sql

Stage-oriented SQL files used for schema creation, data import templates, and sanity checks.

These files mirror the migration-based implementation and are provided for Stage 1 reviewability.

Stage 2 Hive/Spark SQL assets:

- `stage2_hive_init.hql` - Hive DDL for external and optimized tables.
- `q1.hql`, `q2.hql`, `q3.hql` - EDA insight queries.
